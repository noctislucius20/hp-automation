---
- hosts: workstation_deploy
  become: true
  tasks:
  - name: update repo cache
    apt:
      update_cache: yes
    changed_when: false
      
- hosts: workstation_deploy
  roles:
    - minikube
    - awx
    - postgres
    - elk
    - filebeat
  tags: [ workstation ]